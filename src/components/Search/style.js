import styled from "styled-components";

export const Search = styled.div`
  position: relative;
  width: 100%;
  max-width: 500px;
  margin: 20px auto;

  & .textfield {
    padding-right: 68px;
  }
  & .search-button {
    position: absolute;
    top: 0;
    right: 0;
  }
`;
